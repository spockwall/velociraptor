//! Executor binary entrypoint. Phase 3 will wire up the ZMQ REP loop,
//! per-exchange REST clients, and the dead-man-switch heartbeat.

fn main() {
    eprintln!("executor: not yet implemented (Phase 3)");
}
